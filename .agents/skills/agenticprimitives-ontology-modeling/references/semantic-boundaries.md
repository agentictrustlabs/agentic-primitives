# Semantic boundaries

- OWL/RDFS: meaning and entailment under open-world assumptions
- SHACL: graph constraints under a declared validation regime
- JSON Schema/Zod: application payload validation
- TypeScript: compile-time application shape
- Delegation/policy/contracts: execution authority

Do not collapse these layers into one another.
